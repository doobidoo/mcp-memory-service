#!/usr/bin/env bash

# Use direnv's built-in Python virtual environment support
layout python venv/bin/python

# Optional: Set project-specific environment variables
export MCP_MEMORY_STORAGE_BACKEND=chroma
export MCP_MEMORY_CHROMA_PATH="/Users/selasi/.mcp_memory_shared"
export MCP_MEMORY_BACKUPS_PATH="$HOME/.mcp_memory_backups"
